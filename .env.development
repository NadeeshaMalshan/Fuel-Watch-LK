# Frontend → hosted API (no local server required for npm run dev).
# To use local Express + Vite proxy instead: delete this file or use .env.development.local with:
# VITE_API_URL=
VITE_API_URL=https://fuel-watch-lk-production.up.railway.app/api
